# Если скопировать основной список через .copy(), то копия будет содержать не значения, а такие же ссылки как и оригинал
# Для полного копирования списков есть смысл копировать с помощью функции deepcopy()
# Импорт:  from copy import deepcopy